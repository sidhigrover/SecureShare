PORT=4000
# Redis URL: e.g. redis://localhost:6379 or rediss://:<password>@host:port
# Use memory:// for Railway deployment without Redis
REDIS_URL=memory://
# CORS allowed origins (comma-separated). Default: * for dev
CORS_ORIGINS=
